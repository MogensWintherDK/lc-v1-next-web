/** @type {import('next-sitemap').IConfig} */
// Default code you can customize according to your requirements.
module.exports = {
    siteUrl: process.env.LNX_HOSTNAME || 'https://example.com',
    generateRobotsTxt: true, // (optional)
    // REST CODE READ DOCS  ...
}